# 终端环境配置脚本使用说明

## 🚀 快速开始

```bash
cd /home/onewu/Downloads/archlinux-main/script
./setup-terminal.sh
```

---

## 📦 脚本清单

### 1. setup-terminal.sh (主脚本) ⭐
**大小**: 33K  
**行数**: 1203 行  
**功能**: 完整的终端环境配置工具，集成所有功能

**菜单选项**:
1. **完整安装** - 适合新系统，安装所有组件
2. **安装现代化命令行工具** - lsd, bat, eza, btop, ripgrep, fd, fzf, zoxide 等
3. **安装/切换 Zsh 主题** - Powerlevel10k, Starship, Spaceship
4. **修复 Shell 配置冲突** - 修复 bash/zsh 配置问题
5. **配置中文输入法** - fcitx5 拼音输入法
6. **配置终端启动信息** - fastfetch 简洁显示
7. **Shell 状态检查与切换** - 查看和切换当前 shell
8. **修复 Chrome 中文输入问题** - 修复浏览器输入法
9. **生成 SSH 密钥** - Ed25519 或 RSA 密钥生成 🆕
10. **查看系统信息** - 显示系统和工具状态 🆕
0. **退出**

---

### 2. fix-shell-config.sh
**大小**: 4.2K  
**功能**: 修复 bash/zsh 配置冲突

---

### 3. fix-chrome-input.sh
**大小**: 4.2K  
**功能**: 修复 Chrome/Google 浏览器中文输入问题

---

## 🚀 快速开始

### 新系统完整安装
```bash
cd /home/onewu/Downloads/archlinux-main/script
./setup-terminal.sh
# 选择 1 - 完整安装
```

### 只安装现代化工具
```bash
./setup-terminal.sh
# 选择 2 - 安装现代化命令行工具
```

### 修复配置问题
```bash
./fix-shell-config.sh
# 或
./setup-terminal.sh
# 选择 4 - 修复 Shell 配置冲突
```

---

## 📝 常见问题

### Q1: 出现 "bash: setopt: 未找到命令" 错误
**原因**: 在 bash 中加载了 zsh 配置

**解决**:
```bash
# 方法1: 切换到 zsh
zsh

# 方法2: 重载正确的配置
source ~/.bashrc

# 方法3: 运行修复脚本
./fix-shell-config.sh
```

### Q2: 颜色代码显示为乱码
**原因**: echo 命令缺少 -e 参数

**解决**: 所有脚本已修复，重新运行即可

### Q3: 如何切换默认 shell？
```bash
# 查看当前 shell
echo $SHELL

# 切换到 zsh
chsh -s $(which zsh)

# 或使用菜单选项 7
./setup-terminal.sh
```

### Q4: 中文输入法快捷键是什么？
- **Ctrl + Space** - 切换中文/英文输入

### Q5: 如何配置 Powerlevel10k 主题？
```bash
# 安装主题后运行
p10k configure
```

---

## 🎨 安装后的别名

### 文件操作
```bash
ll      # lsd -lah (详细列表)
lt      # lsd --tree (树形显示)
cat     # bat (语法高亮)
```

### 系统监控
```bash
top     # btop (现代化监控)
df      # duf (磁盘使用)
du      # ncdu (磁盘分析)
```

### 搜索工具
```bash
grep    # rg (ripgrep)
find    # fd (快速查找)
cd      # z (智能跳转，使用 zoxide)
```

### 访问原始命令
```bash
oldgrep # 原始 grep
oldfind # 原始 find
catt    # 原始 cat
```

---

## 🔧 配置文件位置

- **zsh 配置**: `~/.zshrc`
- **bash 配置**: `~/.bashrc`
- **fastfetch 配置**: `~/.config/fastfetch/config.jsonc`
- **fcitx5 配置**: `~/.config/fcitx5/`
- **starship 配置**: `~/.config/starship.toml`

---

## 📞 重要命令

```bash
# 重载配置
source ~/.zshrc      # zsh
source ~/.bashrc     # bash

# 编辑配置
zshconfig           # 编辑 .zshrc
zshreload           # 重载 .zshrc

# 切换 shell
zsh                 # 临时切换到 zsh
bash                # 临时切换到 bash

# 系统信息
fastfetch           # 显示系统信息
```

---

## ✅ 安装验证

安装完成后，验证以下内容:

1. ✅ 运行 `zsh` 无错误
2. ✅ 运行 `ll` 显示彩色文件列表
3. ✅ 按 `Ctrl+Space` 可切换中文输入
4. ✅ 打开新终端显示系统信息
5. ✅ 运行 `cat file` 有语法高亮

---

## 🎯 推荐流程

### 首次安装
1. 运行 `./setup-terminal.sh`
2. 选择 `1` 完整安装
3. 等待安装完成
4. 注销并重新登录
5. 享受新的终端环境！

### 已有配置，只想安装工具
1. 运行 `./setup-terminal.sh`
2. 选择 `2` 安装现代化工具
3. 运行 `source ~/.zshrc` 重载配置

### 遇到配置冲突
1. 运行 `./fix-shell-config.sh`
2. 或运行 `./setup-terminal.sh` 选择 `4`
3. 按提示操作

---

## 📦 安装的工具列表

### 现代化命令行工具
- **lsd** - 现代化 ls，彩色图标显示
- **bat** - 现代化 cat，语法高亮
- **eza** - 另一个现代化 ls
- **btop** - 现代化 top，资源监控
- **htop** - 交互式进程查看器
- **ripgrep (rg)** - 超快搜索工具
- **fd** - 现代化 find
- **fzf** - 模糊搜索
- **zoxide** - 智能 cd，记忆常用目录
- **tldr** - 简化版 man 页面
- **ncdu** - 磁盘使用分析
- **duf** - 现代化 df
- **dust** - 现代化 du

### 开发工具
- **lazygit** - Git TUI
- **diff-so-fancy** - 更好的 git diff
- **neovim** - 现代化 vim

### 支持的 Zsh 主题
1. **Powerlevel10k** - 功能强大，高度可定制（推荐）
2. **Starship** - 跨 shell，极快
3. **Spaceship** - 简洁美观

---

**最后更新**: 2025-11-23  
**维护**: 简化版本，只保留必要脚本

---

## 🚀 快速开始

### 新系统完整安装
```bash
cd /home/onewu/Downloads/archlinux-main/script
./setup-terminal.sh
# 选择 1 - 完整安装
```

### 只安装现代化工具
```bash
./setup-terminal.sh
# 选择 2 - 安装现代化命令行工具
```

### 修复配置问题
```bash
./fix-shell-config.sh
# 或
./setup-terminal.sh
# 选择 4 - 修复 Shell 配置冲突
```

---

## 📝 常见问题

### Q1: 出现 "bash: setopt: 未找到命令" 错误
**原因**: 在 bash 中加载了 zsh 配置

**解决**:
```bash
# 方法1: 切换到 zsh
zsh

# 方法2: 重载正确的配置
source ~/.bashrc

# 方法3: 运行修复脚本
./fix-shell-config.sh
```

### Q2: 颜色代码显示为乱码
**原因**: echo 命令缺少 -e 参数

**解决**: 所有脚本已修复，重新运行即可

### Q3: 如何切换默认 shell？
```bash
# 查看当前 shell
echo $SHELL

# 切换到 zsh
chsh -s $(which zsh)

# 或使用交互式工具
./switch-shell.sh
```

### Q4: 中文输入法快捷键是什么？
- **Ctrl + Space** - 切换中文/英文输入

### Q5: 如何配置 Powerlevel10k 主题？
```bash
# 安装主题后运行
p10k configure
```

---

## 🎨 安装后的别名

### 文件操作
```bash
ll      # lsd -lah (详细列表)
lt      # lsd --tree (树形显示)
cat     # bat (语法高亮)
```

### 系统监控
```bash
top     # btop (现代化监控)
df      # duf (磁盘使用)
du      # ncdu (磁盘分析)
```

### 搜索工具
```bash
grep    # rg (ripgrep)
find    # fd (快速查找)
cd      # z (智能跳转，使用 zoxide)
```

### 访问原始命令
```bash
oldgrep # 原始 grep
oldfind # 原始 find
catt    # 原始 cat
```

---

## 🔧 配置文件位置

- **zsh 配置**: `~/.zshrc`
- **bash 配置**: `~/.bashrc`
- **fastfetch 配置**: `~/.config/fastfetch/config.jsonc`
- **fcitx5 配置**: `~/.config/fcitx5/`
- **starship 配置**: `~/.config/starship.toml`

---

## 📞 重要命令

```bash
# 重载配置
source ~/.zshrc      # zsh
source ~/.bashrc     # bash

# 编辑配置
zshconfig           # 编辑 .zshrc
zshreload           # 重载 .zshrc

# 切换 shell
zsh                 # 临时切换到 zsh
bash                # 临时切换到 bash

# 系统信息
fastfetch           # 显示系统信息
```

---

## ✅ 安装验证

安装完成后，验证以下内容:

1. ✅ 运行 `zsh` 无错误
2. ✅ 运行 `ll` 显示彩色文件列表
3. ✅ 按 `Ctrl+Space` 可切换中文输入
4. ✅ 打开新终端显示系统信息
5. ✅ 运行 `cat file` 有语法高亮

---

## 🎯 推荐流程

### 首次安装
1. 运行 `./setup-terminal.sh`
2. 选择 `1` 完整安装
3. 等待安装完成
4. 注销并重新登录
5. 享受新的终端环境！

### 已有配置，只想安装工具
1. 运行 `./setup-terminal.sh`
2. 选择 `2` 安装现代化工具
3. 运行 `source ~/.zshrc` 重载配置

### 遇到配置冲突
1. 运行 `./fix-shell-config.sh`
2. 或运行 `./setup-terminal.sh` 选择 `4`
3. 按提示操作

---

**最后更新**: 2025-11-23
